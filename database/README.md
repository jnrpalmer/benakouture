# Bena Kouture - Database

MongoDB is used for this project. Configure `MONGODB_URI` in `backend/.env`.

Default: `mongodb://localhost:27017/bena-kouture`

The backend will create collections: `users`, `products`, `orders`.
